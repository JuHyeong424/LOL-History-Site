import colors from '@/styles/theme/colors.ts';
import spacing from '@/styles/theme/spacing.ts';
import typography from '@/styles/theme/typography.ts';
import viewSize from '@/styles/theme/viewSize.ts';
import viewport from '@/styles/theme/viewport.ts';

const theme = {
  colors,
  spacing,
  typography,
  viewSize,
  viewport,
};

export default theme;
