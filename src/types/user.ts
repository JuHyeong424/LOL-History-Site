export interface PuuidData {
  puuid: string;
  gameName: string;
  tagLine: string;
}

export interface UserData {
  puuid: string;
  profileIconId: number;
  revisionDate: number;
  summonerLevel: number;
}
