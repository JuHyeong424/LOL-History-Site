import { HeaderWrapper, TopHeader } from '@/components/styles/header.sytles.ts';

export default function Header() {
  return (
    <HeaderWrapper>
      <TopHeader>롤 전적 검색 사이트</TopHeader>
    </HeaderWrapper>
  );
}
